#!/usr/bin/env bash
set -euo pipefail

VERSION_FILE="package.json"

if [[ ! -f "$VERSION_FILE" ]]; then
    echo "[pre-commit] $VERSION_FILE not found"
    exit 0
fi

version=$(node -p "require('./$VERSION_FILE').version")

echo "[pre-commit] committing version $version"
